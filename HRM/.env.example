# Mẫu cấu hình cho docker-compose.
# Copy file này thành .env rồi điền mật khẩu thật: cp .env.example .env
# File .env thật KHÔNG được commit lên git.
MYSQL_ROOT_PASSWORD=your_password_here
MYSQL_DATABASE=hrm
DB_USERNAME=root
DB_PASSWORD=your_password_here
